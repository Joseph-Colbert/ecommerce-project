import { EnterpriseCategory } from './enterprise-category';

describe('EnterpriseCategory', () => {
  it('should create an instance', () => {
    expect(new EnterpriseCategory()).toBeTruthy();
  });
});
