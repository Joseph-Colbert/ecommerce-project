import { OrderItemOnCredit } from './order-item-on-credit';

describe('OrderItemOnCredit', () => {
  it('should create an instance', () => {
    expect(new OrderItemOnCredit()).toBeTruthy();
  });
});
