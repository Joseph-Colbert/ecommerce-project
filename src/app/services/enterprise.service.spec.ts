import { Enterprise.Service } from './enterprise.service';

describe('Enterprise.Service', () => {
  it('should create an instance', () => {
    expect(new Enterprise.Service()).toBeTruthy();
  });
});
